version: "3.8"

services:
  order-service:
    build: .
    ports:
      - "3003:3003"
    env_file:
      - .env
    environment:
      PORT: ${PORT}
      DATABASE_URL: ${DATABASE_URL}
      ORDER_URL: ${ORDER_URL}
      NOTIFICATION_URL: ${NOTIFICATION_URL}
