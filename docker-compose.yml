version: "3.8"

services:
  delivery-api:
    build: .
    ports:
      - "3003:3003"
    env_file:
      - .env
    environment:
      PORT: 3003
      DATABASE_URL: ${DATABASE_URL}
      ORDER_API_URL: ${ORDER_API_URL}
      NOTIFICATION_API_URL: ${NOTIFICATION_API_URL}
    networks:
      - shoong-network

networks:
  shoong-network:
    external: true